/*
 * mqtt_pal.c — Platform abstraction layer stub for vendored mqtt-c.
 *
 * The emd project uses its own socket management (emd_net.c / emd_mqtt.c).
 * This file exists to satisfy the cmake target; no symbols are needed
 * beyond those already provided by mqtt.c.
 */

/* Intentionally empty — no platform-specific symbols required. */
